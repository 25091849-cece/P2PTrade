# P2PTrade

## Contribution workflow

This repository uses a protected `main` branch workflow:

- Contributors must create `feature/<name>` branches.
- All changes must be submitted as pull requests to `main`.
- Code owner approval is required before merge.
- Only the repository owner/maintainer merges into `main`.

See `CONTRIBUTING.md` for contribution rules.
